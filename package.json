{
  "name": "chess",
  "version": "1.0.0",
  "description": "Obsidian plugin: play chess against a bot with adjustable 1-10 difficulty.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && npm test && node esbuild.config.mjs production",
    "test": "node tests/run.mjs",
    "vendor": "node vendor-pieces.mjs"
  },
  "keywords": [
    "obsidian-plugin",
    "chess",
    "game"
  ],
  "author": "mrrepac",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.6",
    "esbuild": "0.17.3",
    "obsidian": "latest",
    "tslib": "2.4.0",
    "typescript": "4.7.4"
  },
  "//": "chess.js is pinned exactly: the search uses its internals (see src/engine-board.ts), which a minor bump could rename. tests/engine.test.mjs guards any upgrade.",
  "dependencies": {
    "chess.js": "1.4.0"
  }
}
